import { user } from "./userService.js";


const sections = document.querySelector('.section');

/*
user.getAll().then(data => {
    console.log(data);
    
    sections.innerHTML = '';

    data.forEach((element, index, array) => {
        sections.innerHTML += 
        `<div class="item" style=" display: inline-block; padding: 5px" border: 1px solid blue;" >
        <h3>${element.IME}</h3>
        <p>${element.OPIS}</p>
        <p>${element.CENA}</p>
        </div>
        `;
    });


})
*/
























